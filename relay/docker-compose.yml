services:
  relay:
    build:
      context: ..
      dockerfile: relay/Dockerfile
    ports:
      - "${RELAY_EXTERNAL_PORT:-28186}:8080"
    environment:
      - RELAY_PORT=8080
    restart: unless-stopped
